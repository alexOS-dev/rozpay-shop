import { Inter, Allerta_Stencil } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], display: 'swap' });

export const titleFont = Allerta_Stencil({
  subsets: ['latin'],
  weight: ['400'],
  display: 'swap',
});
